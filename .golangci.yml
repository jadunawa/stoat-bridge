version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - typecheck
  settings:
    errcheck:
      check-type-assertions: true

issues:
  max-issues-per-linter: 50
  max-same-issues: 5
